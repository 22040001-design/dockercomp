---
- hosts: all
  tasks:

    - name: Install python3-apt
      apt:
        name: python3-apt
        state: present
        update_cache: yes

    - name: Update apt cache
      apt:
        update_cache: yes

    - name: Upgrade all packages
      apt:
        upgrade: dist
